import { CheckActiveDayDirective } from './check-active-day.directive';

describe('CheckActiveDayDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckActiveDayDirective();
    expect(directive).toBeTruthy();
  });
});
