import { StarsDirective } from './stars.directive';

describe('StarsDirective', () => {
  it('should create an instance', () => {
    const directive = new StarsDirective();
    expect(directive).toBeTruthy();
  });
});
